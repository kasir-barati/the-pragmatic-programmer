# Genral Tools

- [GPU info](./gpu_info.py).
- [Cleanup local machine from project untracked files/directories](./cleanup.sh):
  ```shell
  touch cleanup.sh
  # Now copy and paste the content in it.
  chmod +x cleanup.sh
  sudo mv cleanup.sh /usr/local/bin/cleanup
  ```
- [Change the commit of last commits](./change-git-commits-email.sh):
  ```shell
  touch change-git-commits-email.sh
  chmod +x change-git-commits-email.sh
  sudo mv change-git-commits-email.sh /usr/local/bin/change-git-commits-email
  ```

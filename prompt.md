I wanna have a bash script to auto configure a freshly installed Ubunut using what we have in`customize-your-dev-env`:

- Install and configure Docker.
- Install nvm.
- Use nvm to install node 24.
- Install uv.
- Install zsh.
- Configure it to use use zsh.
- Install flameshot.
- Install gimp using snap.
- Install vlc using snap.
- Install vscode using snap.
- Install brave using snap.
- Install build-essential, ffmpeg .
- Install Postman using snap.
- Install & configure pnpm like what we have in @/customize-your-dev-env/pnpm 

But I guess we need to install all the setup we have for the Browser (brave), VSCode